This app is made for learning purpuses

The dependencies needed to run this program are:

github.com/joho/godotenv
github.com/lib/pq

After you install the dependencies, the command to run this code is:

go run .
